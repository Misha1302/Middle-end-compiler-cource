# Занятие 1. Путь программы через компилятор

> Не список английских слов, а цепочка представлений и проверяемых гарантий

[← Карта курса](../course-map.md) · [Следующее занятие](../modules/02-leaders-and-basic-blocks.md)

## Результат занятия

Ты объясняешь, зачем появляется каждая стадия от исходного текста до запуска программы, и различаешь анализ имён, типов и эффектов.

**Перед началом:** достаточно уметь читать маленькие фрагменты кода; остальные термины вводятся здесь.

## Зачем это вообще нужно

Исходный текст удобен человеку, но неудобен оптимизатору и машине. В тексте ещё неясно, к какому объявлению относится имя, корректны ли типы операций и может ли инструкция менять состояние программы. Компилятор постепенно заменяет неявные свойства явными структурами.

Главное правило курса: следующая стадия не должна угадывать то, что обязана была проверить предыдущая.

## Термины до заданий

| Термин | Простое объяснение | Точная опора |
|---|---|---|
| **frontend — передняя часть** | понимает исходный язык и сообщает ошибки | лексер, парсер, разрешение имён, проверка типов и диагностика |
| **middle-end — средняя часть** | анализирует и оптимизирует программу независимо от конкретного процессора | работает с IR, CFG, SSA и машинно-независимыми проходами |
| **backend — задняя часть** | выражает результат средствами целевой машины | понижение, выбор инструкций, регистры, ассемблер и объектный код |
| **IR — промежуточное представление** | внутренний язык компилятора | структура с явным контрактом, удобная конкретному анализу или преобразованию |
| **линковка** | соединение объектных файлов и библиотек | разрешает внешние символы и формирует исполняемый файл или библиотеку |

Сначала прочитай готовые объяснения. Затем закрой таблицу и сформулируй каждый термин своими словами. Пустая формулировка ученика — это проверка после обучения, а не замена обучения.

## Первая модель

```mermaid
flowchart LR
  S[Исходный текст] --> T[Токены]
  T --> AST[AST]
  AST --> SEM[Имена, типы, эффекты]
  SEM --> IR[IR]
  IR --> CFG[Блоки и CFG]
  CFG --> SSA[SSA и анализы]
  SSA --> OPT[Оптимизации]
  OPT --> LOW[Понижение и backend]
  LOW --> OBJ[Объектный файл]
  OBJ --> LINK[Линковка]
  LINK --> EXE[Исполняемый файл]
  EXE --> RUN[Загрузчик и runtime]
```

**Что означает `name, type and effect analysis`:**

- **анализ имён** связывает каждое использование `x` с конкретным параметром, локальной переменной или функцией;
- **анализ типов** проверяет допустимость операций и назначает тип результату;
- **анализ эффектов** отмечает чтение/запись памяти, ввод-вывод, возможное исключение и неизвестный вызов.

Это логическая группа проверок. В конкретном компиляторе она может быть разбита на несколько проходов.

## Разобранный пример: состояние за состоянием

Исходник:

```c
int f(int x) {
    int y;
    if (x > 0) y = x + 1;
    else       y = 1 - x;
    return y * 2;
}
```

| Шаг | Представление | Что стало явным |
|---|---|---|
| 1 | токены `int`, `f`, `(`, `x`, ... | границы слов и знаков |
| 2 | AST функции, условия и двух присваиваний | синтаксическая структура |
| 3 | оба `x` связаны с параметром; операции имеют тип `int`; эффектов памяти нет | семантика имён, типов и эффектов |
| 4 | линейный IR с метками и `goto` | порядок вычислений и переходы |
| 5 | базовые блоки и CFG | возможные пути управления |
| 6 | `y1`, `y2`, `y3 = φ(...)` | происхождение каждого значения `y` |
| 7 | оптимизированный IR | удалённые или упрощённые вычисления |
| 8 | машинные инструкции и объектный файл | привязка к целевой архитектуре |
| 9 | исполняемый файл | внешние ссылки разрешены линкером |

## Формальное правило

Для каждой границы отвечай на четыре вопроса:

1. Что является входом?
2. Что является выходом?
3. Какую неопределённость устраняет стадия?
4. Какую гарантию имеет право использовать следующая стадия?

Название компонента без этих четырёх ответов ещё не доказывает понимание pipeline.

## Типичные ошибки

- Считать frontend только парсером и забывать про имена, типы и диагностику.
- Считать анализ эффектов оптимизацией: сначала это факт о поведении инструкции, затем его используют оптимизации.
- Путать объектный файл с готовым исполняемым файлом.
- Считать interpreter стадией линковки: интерпретатор — другой способ исполнения IR.

## Задача A — по образцу

Заполни таблицу для стадий `source → tokens → AST → semantic analysis → IR → CFG → SSA → optimized IR → object → executable`:

| Стадия | Вход | Выход | Новая гарантия |
|---|---|---|---|
| tokens |  |  |  |
| AST |  |  |  |
| semantic analysis |  |  |  |
| CFG |  |  |  |
| SSA |  |  |  |
| linking |  |  |  |

<details>
<summary>Проверка задачи A — открывать после попытки</summary>

Возможный ответ:

| Стадия | Вход | Выход | Новая гарантия |
|---|---|---|---|
| tokens | символы | токены | слова и знаки разделены |
| AST | токены | дерево синтаксиса | структура соответствует грамматике |
| semantic analysis | AST и таблицы объявлений | типизированное связанное дерево/IR | имена разрешены, типы проверены, эффекты описаны |
| CFG | линейный IR | блоки и рёбра | каждый возможный непосредственный переход отражён |
| SSA | CFG и определения | SSA-IR | каждое SSA-имя определяется один раз |
| linking | объектные файлы и библиотеки | executable/shared object | внешние символы разрешены либо выдана ошибка |

</details>

## Задача B — перенос на новый пример

Для операций ниже укажи, какая часть semantic analysis отвечает за факт:

1. `x + 1`, где `x` не объявлен;
2. `1 + "abc"`;
3. `print(x)`;
4. `10 / y`;
5. вызов неизвестной функции `g()`.

<details>
<summary>Проверка задачи B — открывать после попытки</summary>

1. Анализ имён: `x` не связан с объявлением.  
2. Анализ типов: операнды несовместимы для выбранного `+`.  
3. Анализ эффектов: есть наблюдаемый вывод.  
4. Анализ типов подтверждает числовую операцию; анализ эффектов/исключений отмечает возможную ошибку при нуле.  
5. Разрешение имён ищет `g`; эффект неизвестного вызова обычно считается консервативно наблюдаемым.

</details>

## Проверка на выходе

**Почему одного AST недостаточно middle-end?**  


**Чем анализ типа отличается от анализа эффекта?**  


**Почему объектный файл ещё не всегда можно запустить?**  


<details>
<summary>Короткие ответы</summary>

**Почему одного AST недостаточно middle-end?**  
AST отражает синтаксис, но обычно не делает явными CFG, SSA и удобные для оптимизаций зависимости.

**Чем анализ типа отличается от анализа эффекта?**  
Тип отвечает, какие значения и операции допустимы; эффект — что операция наблюдаемо делает с состоянием или управлением.

**Почему объектный файл ещё не всегда можно запустить?**  
В нём могут оставаться внешние символы и релокации; их разрешает линкер.

</details>

## Профессиональная граница

Реальные toolchain объединяют и переставляют стадии. Важна не картинка из учебника, а фактический контракт каждого представления.
