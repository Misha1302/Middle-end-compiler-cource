# Эталоны и критерии проверки

Эта страница помогает проверять решения после самостоятельной попытки. Она не заменяет пошаговые разборы внутри занятий.

## Общий принцип

Сначала найди самое раннее ошибочное состояние. Поздние расхождения часто являются его следствиями. Если ранний этап неверен, разрешается продолжить с явно подписанным исправленным допущением; такой ответ получает баллы за последующие независимо выполненные этапы.

## Базовые блоки, CFG и доминирование

Проверь, что:

- лидеры найдены по правилам линейного IR;
- каждый fall-through после нормализации заменён явным `jump`;
- каждый нормализованный блок имеет ровно один терминатор;
- достижимость считается от входного блока;
- `pred` и `succ` симметричны;
- CFG и дерево доминаторов нарисованы раздельно;
- каждое утверждение о доминировании подтверждено определением или путём-контрпримером;
- каждый элемент DF имеет конкретного предшественника-свидетеля;
- недостижимые блоки не участвуют в обязательных результатах анализа, если явно не сказано иное.

## SSA

Проверь, что:

- расстановка φ-функций и переименование показаны как разные этапы;
- каждое SSA-имя определяется ровно один раз;
- обычное определение доминирует все обычные использования;
- у каждой φ-функции есть один подписанный вход для каждого достижимого предшественника блока;
- операнд φ-функции доступен на соответствующем входящем ребре;
- после изменения CFG заново проверены φ-функции и доминирование;
- используется единая запись `x3 = φ[B1: x1, B2: x2]`.

## Оптимизации без циклов

Проверь, что:

- используется семантика Course IR v1 или явно записаны отличия;
- до преобразования названы тип, правила переполнения, ошибки и предположения об эффектах;
- LVN не используется как название глобального преобразования между блоками;
- доказательство допустимости не подменено ожидаемой выгодой;
- после удаления недостижимых блоков исправлены рёбра, `pred/succ` и φ-функции;
- факты о памяти не переносятся через неизвестную запись или вызов без отдельного доказательства;
- сохранена версия IR после каждого прохода;
- перечислены сохранённые и устаревшие анализы.

## Циклы

Проверь, что:

- обратное ребро естественного цикла доказано доминированием, а не направлением стрелки;
- естественный цикл собран обратным обходом и не включает предвходной блок;
- несводимость подтверждена разными входными вершинами и отсутствием единого доминирующего заголовка;
- количество внешних рёбер не подменяет количество входных вершин;
- снижение стоимости операций проверено на начальном значении и переходе между итерациями;
- для LICM отдельно доказаны неизменность, эффекты и память, безопасность раннего выполнения и доминирование использований;
- каждое преобразование цикла сопровождается зависимостями, границами, остаточными итерациями и выводом о допустимости.

## Аналитическая шкала комплексной работы

Оцени каждый пункт отдельно. Значение пункта нельзя компенсировать красивым итогом другого этапа.

| Результат | Баллы | Условие полного балла |
|---|---:|---|
| лидеры и блоки | 4 | все границы верны, блоки максимальны |
| нормализация терминаторов | 3 | каждый fall-through превращён в правильный `jump` |
| CFG, `pred/succ` | 5 | все и только возможные рёбра, таблицы симметричны |
| достижимость | 2 | обход из entry и явное множество посещённых |
| Dom | 6 | полные множества или корректная трасса неподвижной точки |
| `idom` и дерево | 4 | ближайшие строгие доминаторы и отдельное дерево |
| DF | 6 | все множества и свидетели-предшественники |
| расстановка φ | 5 | рабочая очередь и все необходимые φ без лишнего смешения с renaming |
| переименование SSA | 7 | версии, стеки, входы φ и удаления из стеков |
| verifier SSA | 4 | определения, доминирование, φ-рёбра |
| ациклические проходы | 8 | IR после каждого шага и точная допустимость |
| структура цикла | 5 | header, latch, preheader, выходы, natural/irreducible verdict |
| индуктивные переменные | 5 | начальное значение, шаг, замкнутая и рекуррентная формы |
| LICM/преобразования | 8 | все предусловия и вывод отдельно от выгодности |
| invalidation | 4 | сохранённые и устаревшие анализы после каждого изменения |
| итоговая проверка | 4 | повторная проверка CFG, SSA, типов и эффектов |
| **Итого** | **80** |  |

## Правила частичного зачёта

- **Полный балл пункта:** результат верен, показаны необходимые промежуточные состояния и проверка.
- **Половина баллов:** основная структура верна, но пропущен один существенный шаг или свидетель.
- **Ноль за пункт:** результат основан на догадке, противоречит условию или нарушает основной инвариант.
- Ошибка в CFG не обнуляет автоматически поздние этапы, если ученик явно продолжил с корректным предоставленным CFG.
- Верный финальный IR без промежуточных доказательств не получает полный балл за пропущенные этапы.
- Альтернативные имена блоков и SSA-версий допустимы, если структура и соответствия однозначны.

## Условие прохождения

Для учебной попытки достаточно не менее 56 из 80 баллов при двух дополнительных требованиях:

1. CFG и SSA verifier не содержат неотмеченного критического нарушения;
2. ни одно преобразование с памятью или возможной ошибкой не объявлено допустимым без необходимых доказательств.
